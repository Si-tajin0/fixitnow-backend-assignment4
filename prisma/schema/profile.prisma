model TecnicianProfile {
  id         String   @id @default(uuid())
  skills     String[]
  experience Int
  pricing    Float

  // Relation Field
  userId String @unique
  user   User   @relation(fields: [userId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("technician_profiles")
}
